# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ContactsTestTask' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ContactsTestTask
  
  pod 'RxSwift',    '~> 3.0'
  pod 'RxCocoa',    '~> 3.0'
  pod 'ObjectMapper', '~> 2.2'
  pod 'DTAlertViewContainer', '~> 0.1.6'
  
  target 'ContactsTestTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ContactsTestTaskUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
